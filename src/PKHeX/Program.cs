// Minimal entry point for WASM library
// The actual API is exposed via JSExport attributes in the Api folder
class Program
{
    static void Main()
    {
        // Empty entry point - library functions are called via JSExport
    }
}
